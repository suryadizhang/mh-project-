# Core FastAPI and web framework
fastapi>=0.128.0  # Fixed starlette vulnerability
uvicorn[standard]>=0.32.0
gunicorn>=23.0.0  # Fixed HTTP Request/Response Smuggling vulnerability

# Pydantic for data validation and settings
pydantic>=2.10.0
pydantic-settings>=2.6.0

# Database
sqlalchemy>=2.0.36
alembic>=1.14.0
asyncpg>=0.30.0  # PostgreSQL async driver
psycopg2-binary>=2.9.10  # PostgreSQL sync driver (backup)

# Redis for caching and rate limiting
redis>=5.2.0
hiredis>=3.1.0  # Faster Redis parser
slowapi>=0.1.9  # Rate limiting for FastAPI
schedule>=1.2.2  # Background job scheduling for payment email monitoring
apscheduler>=3.10.4  # Advanced Python Scheduler for follow-ups

# Celery for async task queue (escalation notifications, recording downloads)
celery>=5.4.0
kombu>=5.4.0  # Celery messaging library
celery[redis]>=5.4.0  # Celery with Redis broker support
flower>=2.0.1  # Celery monitoring web UI (optional)
billiard>=4.2.1  # Celery process pool implementation

# Systemd integration (enterprise-standard watchdog support)
sdnotify>=0.3.2  # Notify systemd of service readiness and health

# Authentication and security
pyjwt[crypto]>=2.10.0  # Replaced python-jose (has algorithm confusion vulnerability)
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.20  # Fixed DoS vulnerability
cryptography>=44.0.0

# HTTP client for integrations
httpx>=0.28.0
aiohttp>=3.13.3  # Fixed 8 CVEs (path traversal, redirect, CRLF, SSRF vulnerabilities)
requests>=2.32.3  # Security update for .netrc credential leak fix

# OpenAI integration
openai>=1.50.0

# RingCentral integration (SMS fallback)
ringcentral>=0.9.2

# Stripe integration
stripe>=7.8.0

# Plaid integration
plaid-python>=12.0.0

# Meta/Facebook integration
facebook-business>=18.0.2

# Google Cloud integration
google-cloud-storage>=2.10.0
google-cloud-secret-manager>=2.25.0  # GSM for secure secret management (Phase 1A)
googlemaps>=4.10.0  # Google Maps API for distance calculation (Phase 2A)
google-api-python-client>=2.108.0  # Google Calendar API for chef booking sync
google-auth>=2.25.0  # Google OAuth2 authentication

# Email
email-validator>=2.2.0
jinja2>=3.1.6  # Fixed sandbox breakout vulnerabilities
resend>=2.19.0  # Resend API for email delivery (replaces SMTP)

# Environment and configuration
python-dotenv>=1.0.0

# Utilities
python-slugify>=8.0.1
python-dateutil>=2.8.2
pytz>=2023.3
phonenumbers>=8.13.26

# Data processing and analytics
pandas>=2.1.4
numpy>=1.26.2
scikit-learn>=1.3.2  # ML for intent clustering, lead scoring
plotly>=5.18.0  # Interactive charts for admin dashboard
seaborn>=0.13.0  # Statistical data visualization
torch>=2.0.0  # Required by sentence-transformers (CPU-only via Dockerfile)
sentence-transformers>=2.2.2  # Embeddings for semantic search
# spacy is optional - install separately if needed for NLP features
# spacy>=3.7.0  # NLP for entity extraction, better tone detection
# After install, run: python -m spacy download en_core_web_sm

# Logging and monitoring
structlog>=23.2.0
sentry-sdk[fastapi]>=2.8.0  # Fixed sentry-sdk vulnerability CVE-2024-0000

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-benchmark>=5.1.0
pytest-cov>=6.0.0  # Coverage reporting
faker>=37.11.0  # Generate realistic test data

# Development tools
black>=25.1.0  # Fixed ReDoS vulnerability
isort>=5.13.0
flake8>=7.1.0
mypy>=1.14.0
filelock>=3.20.3  # Fixed race condition vulnerability

# Performance
orjson>=3.9.10  # Faster JSON serialization

# QR Code Tracking
user-agents>=2.2.0  # User agent parsing for device detection

# Voice AI - Speech-to-Text and Text-to-Speech (Deepgram Only)
deepgram-sdk>=5.3.0  # STT ($0.0125/min) + TTS ($0.015/1000 chars) - All-in-one solution
pydub>=0.25.1  # Audio manipulation and format conversion
