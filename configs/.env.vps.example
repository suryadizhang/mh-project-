# =============================================================================
# My Hibachi VPS Environment Configuration
# =============================================================================
# Copy this file to .env.vps and fill in your actual values
#
# NEVER commit .env.vps to version control!
#
# Usage:
#   docker compose -f docker-compose.vps.yml --env-file .env.vps up -d
#
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE - PostgreSQL 13.22 (native on VPS)
# -----------------------------------------------------------------------------
# Production database
DATABASE_URL_PRODUCTION=postgresql+asyncpg://myhibachi_user:<PASSWORD>@host.docker.internal:5432/myhibachi_production
DATABASE_URL_SYNC_PRODUCTION=postgresql+psycopg2://myhibachi_user:<PASSWORD>@host.docker.internal:5432/myhibachi_production

# Staging database
DATABASE_URL_STAGING=postgresql+asyncpg://myhibachi_staging_user:<PASSWORD>@host.docker.internal:5432/myhibachi_staging
DATABASE_URL_SYNC_STAGING=postgresql+psycopg2://myhibachi_staging_user:<PASSWORD>@host.docker.internal:5432/myhibachi_staging

# -----------------------------------------------------------------------------
# REDIS (native on VPS)
# -----------------------------------------------------------------------------
REDIS_URL=redis://host.docker.internal:6379/0
REDIS_URL_STAGING=redis://host.docker.internal:6379/1

# -----------------------------------------------------------------------------
# GOOGLE CLOUD (Secret Manager)
# -----------------------------------------------------------------------------
GCP_PROJECT_ID=my-hibachi-crm
USE_SECRET_MANAGER=true
GOOGLE_APPLICATION_CREDENTIALS_PATH=/root/.config/gcloud/application_default_credentials.json

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
FEATURE_AI_CHAT_ENABLED=false
FEATURE_STRIPE_ENABLED=true
FEATURE_RINGCENTRAL_ENABLED=false

# -----------------------------------------------------------------------------
# OPTIONAL: Override secrets (if not using GSM)
# -----------------------------------------------------------------------------
# STRIPE_SECRET_KEY=sk_live_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx
# OPENAI_API_KEY=sk-xxx
# RESEND_API_KEY=re_xxx
# RINGCENTRAL_JWT_TOKEN=xxx
