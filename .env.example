# My Hibachi Monorepo - Root Environment Variables
# =================================================
#
# This file contains environment variables used at the ROOT level
# (E2E testing, scripts, etc.). Copy this to `.env` and fill in values.
#
# For app-specific variables, see:
#   - apps/backend/.env.example (FastAPI backend)
#   - apps/admin/.env.example (Next.js admin app)
#   - apps/customer/.env.example (Next.js customer app)
#
# =================================================

# ===========================
# E2E Testing (Playwright)
# ===========================

# Vercel Deployment Protection bypass secret
# Get this from: Vercel Dashboard → Project → Settings → Deployment Protection
# Click "Protection Bypass for Automation" → Add Secret → Copy generated value
VERCEL_AUTOMATION_BYPASS_SECRET=

# Override staging URLs (optional - uses defaults if not set)
# STAGING_URL=https://staging.myhibachichef.com
# STAGING_ADMIN_URL=https://admin-staging.mysticdatanode.net
# STAGING_API_URL=https://staging-api.mysticdatanode.net

# ===========================
# Local Development Database
# ===========================

# SSH tunnel to staging database (for local development)
# Start tunnel: ssh -f -N -L 5433:localhost:5432 root@108.175.12.154
# DATABASE_URL_LOCAL=postgresql://myhibachi_staging_user:<STAGING_DB_PASSWORD>@127.0.0.1:5433/myhibachi_staging

# ===========================
# CI/CD (GitHub Actions)
# ===========================

# These are set as repository secrets, not in .env:
# - VERCEL_TOKEN
# - VERCEL_ORG_ID
# - VERCEL_PROJECT_ID_CUSTOMER
# - VERCEL_PROJECT_ID_ADMIN

# ===========================
# E2E Test Credentials
# ===========================
# Super admin credentials for automated testing
# Set these in your local .env file (never commit actual values!)
TEST_ADMIN_EMAIL=
TEST_ADMIN_PASSWORD=

# Database passwords for automated testing (local .env only)
# STAGING_DB_PASSWORD=
# PRODUCTION_DB_PASSWORD=
