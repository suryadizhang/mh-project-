# My Hibachi – Data Flow

**Last Updated:** December 16, 2025 **Purpose:** Document how data
moves through the system

---

## 🌊 Core Data Flows

### 1. Quote → Lead → Booking → Payment

This is the primary customer journey:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    CUSTOMER BOOKING JOURNEY                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐      │
│  │  Quote   │───►│  Lead    │───►│ Booking  │───►│ Payment  │      │
│  │Calculator│    │ Created  │    │ Created  │    │ Processed│      │
│  └────┬─────┘    └────┬─────┘    └────┬─────┘    └────┬─────┘      │
│       │               │               │               │             │
│   User enters     Lead stored     Booking saved   Stripe intent     │
│   guests, zip     with context    customer +      created, paid     │
│                                   event details                     │
│                                                                      │
│  Events:          Events:         Events:         Events:           │
│  - quote_viewed   - lead_created  - booking_new   - payment_intent  │
│                   - funnel_quote  - funnel_book   - deposit_paid    │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

### 2. Quote Calculation Flow

```
Customer                    Frontend                   Backend                    Database
   │                           │                          │                          │
   │  Enter guest count        │                          │                          │
   │  + zip code               │                          │                          │
   │ ─────────────────────────►│                          │                          │
   │                           │                          │                          │
   │                           │  POST /api/quote/calc    │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │                          │  Get station by zip      │
   │                           │                          │─────────────────────────►│
   │                           │                          │◄─────────────────────────│
   │                           │                          │                          │
   │                           │                          │  Get travel fee config   │
   │                           │                          │─────────────────────────►│
   │                           │                          │◄─────────────────────────│
   │                           │                          │                          │
   │                           │                          │  Calculate:              │
   │                           │                          │  - base (adults × $55)   │
   │                           │                          │  - kids (kids × $30)     │
   │                           │                          │  - travel fee            │
   │                           │                          │  - gratuity (20%)        │
   │                           │                          │                          │
   │                           │  { total, breakdown }    │                          │
   │                           │◄─────────────────────────│                          │
   │                           │                          │                          │
   │  Display price            │                          │                          │
   │◄──────────────────────────│                          │                          │
```

---

### 3. Lead Capture Flow

```
Customer                    Frontend                   Backend                    Database
   │                           │                          │                          │
   │  Submit quote form        │                          │                          │
   │  (name, phone, etc.)      │                          │                          │
   │ ─────────────────────────►│                          │                          │
   │                           │                          │                          │
   │                           │  POST /api/leads         │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │                          │  Create lead (crm.leads) │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │                           │                          │  Create contacts         │
   │                           │                          │  (lead.lead_contacts)    │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │                           │                          │  Create context          │
   │                           │                          │  (lead.lead_context)     │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │                           │                          │  Add event: lead_created │
   │                           │                          │  (lead.lead_events)      │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │                           │  { lead_id }             │                          │
   │                           │◄─────────────────────────│                          │
   │                           │                          │                          │
   │                           │  Store lead_id in        │                          │
   │                           │  sessionStorage          │                          │
   │                           │                          │                          │
```

---

### 4. Funnel Event Tracking Flow

```
Customer                    Frontend                   Backend                    Database
   │                           │                          │                          │
   │  Check availability       │                          │                          │
   │ ─────────────────────────►│                          │                          │
   │                           │                          │                          │
   │                           │  GET /api/availability   │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │  (show available slots)  │                          │
   │                           │◄─────────────────────────│                          │
   │                           │                          │                          │
   │                           │  POST /leads/{id}/events │                          │
   │                           │  { "event_type":         │                          │
   │                           │    "funnel_checked_      │                          │
   │                           │     availability" }      │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │                          │  Insert lead_event       │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │  Click "Book Now"         │                          │                          │
   │ ─────────────────────────►│                          │                          │
   │                           │                          │                          │
   │                           │  POST /leads/{id}/events │                          │
   │                           │  { "event_type":         │                          │
   │                           │    "funnel_started_      │                          │
   │                           │     booking" }           │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │  Navigate to /BookUs     │                          │
   │                           │  with pre-filled data    │                          │
   │                           │  from sessionStorage     │                          │
```

---

### 5. Booking Creation Flow

```
Customer                    Frontend                   Backend                    Database
   │                           │                          │                          │
   │  Submit booking form      │                          │                          │
   │ ─────────────────────────►│                          │                          │
   │                           │                          │                          │
   │                           │  POST /api/bookings      │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │                          │  Check slot available    │
   │                           │                          │─────────────────────────►│
   │                           │                          │◄─────────────────────────│
   │                           │                          │                          │
   │                           │                          │  Find/create customer    │
   │                           │                          │  (core.customers)        │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │                           │                          │  Create booking          │
   │                           │                          │  (core.bookings)         │
   │                           │                          │─────────────────────────►│
   │                           │                          │                          │
   │                           │                          │  Create Stripe intent    │
   │                           │                          │──────────►[Stripe]       │
   │                           │                          │◄──────────               │
   │                           │                          │                          │
   │                           │  { booking_id,           │                          │
   │                           │    payment_url }         │                          │
   │                           │◄─────────────────────────│                          │
   │                           │                          │                          │
   │  Redirect to Stripe       │                          │                          │
   │◄──────────────────────────│                          │                          │
```

---

### 6. Payment Webhook Flow

```
Stripe                      Backend                    Database                   Customer
   │                           │                          │                          │
   │  Webhook: payment_intent  │                          │                          │
   │  .succeeded               │                          │                          │
   │ ─────────────────────────►│                          │                          │
   │                           │                          │                          │
   │                           │  Verify webhook sig      │                          │
   │                           │                          │                          │
   │                           │  Find booking by         │                          │
   │                           │  stripe_payment_id       │                          │
   │                           │─────────────────────────►│                          │
   │                           │◄─────────────────────────│                          │
   │                           │                          │                          │
   │                           │  Update booking status   │                          │
   │                           │  → deposit_paid          │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │  Create payment record   │                          │
   │                           │  (core.payments)         │                          │
   │                           │─────────────────────────►│                          │
   │                           │                          │                          │
   │                           │  Send confirmation email │                          │
   │                           │──────────────────────────────────────────────────►│
   │                           │                          │                          │
   │                           │  Send SMS (if consent)   │                          │
   │                           │──────────────────────────────────────────────────►│
   │                           │                          │                          │
   │  200 OK                   │                          │                          │
   │◄──────────────────────────│                          │                          │
```

---

## 🗃️ Data Storage Locations

### Where Data Lives

| Data Type        | Schema.Table                | Notes                 |
| ---------------- | --------------------------- | --------------------- |
| Customer profile | `core.customers`            | Name, email, phone    |
| Booking details  | `core.bookings`             | Event info, pricing   |
| Payment records  | `core.payments`             | Stripe transactions   |
| Lead info        | `crm.leads`                 | Source, status, score |
| Lead contacts    | `lead.lead_contacts`        | Multi-channel         |
| Funnel events    | `lead.lead_events`          | Journey tracking      |
| Chef schedules   | `ops.chefs`                 | Availability          |
| Travel rules     | `pricing.travel_fee_config` | Per-station           |
| User sessions    | Redis                       | JWT tokens            |
| Rate limits      | Redis                       | API throttling        |

---

### Session Data Flow

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│  sessionStorage │────►│   API Request   │────►│    Database     │
│  (browser)      │     │   (backend)     │     │   (PostgreSQL)  │
├─────────────────┤     ├─────────────────┤     ├─────────────────┤
│ - lead_id       │     │ - Validate      │     │ - Persist       │
│ - quote_data    │     │ - Transform     │     │ - Query         │
│ - booking_draft │     │ - Business logic│     │ - Index         │
└─────────────────┘     └─────────────────┘     └─────────────────┘
        │                                                │
        │           ┌─────────────────┐                  │
        └──────────►│      Redis      │◄─────────────────┘
                    ├─────────────────┤
                    │ - Session cache │
                    │ - Rate limits   │
                    │ - Config cache  │
                    └─────────────────┘
```

---

## 🔗 Related Documents

- [ERD](./ERD.md) - Database structure
- [API Contracts](./API_CONTRACTS.md) - Request/response specs
- [Glossary](../00-ONBOARDING/GLOSSARY.md) - Term definitions
